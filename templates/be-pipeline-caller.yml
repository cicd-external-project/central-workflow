name: "BE Pipeline Caller"

# Configure BACKEND_MULTI_SYSTEMS_JSON for NestJS monorepo backends.
# Example:
# [
#   {
#     "name":"backend-api",
#     "dir":".",
#     "install_dir":".",
#     "project":"api",
#     "image":"backend-api",
#     "backend_stack":"nestjs",
#     "version_stream":"api",
#     "test_command":"npm run test:cov -- --selectProjects api",
#     "dockerfile_path":"apps/api/Dockerfile",
#     "k6_script_path":"tests/performance/api-smoke.js"
#   }
# ]

on:
  push:
    branches: [test, uat, main]
  workflow_dispatch:
    inputs:
      pipeline_mode:
        description: "Pipeline mode (auto|single|multi)"
        type: choice
        options: [auto, single, multi]
        default: auto
      enable_security_scan:
        description: "Run backend security scans"
        type: boolean
        default: true
      enable_sonar:
        description: "Run SonarCloud analysis"
        type: boolean
        default: true
      enable_k6:
        description: "Run Grafana k6 performance tests"
        type: boolean
        default: false
      k6_script_path:
        description: "k6 performance test path"
        type: string
        default: "tests/performance"
      k6_base_url:
        description: "Optional override base URL for k6"
        type: string
        default: ""
      k6_run_only_on_branch:
        description: "Allowed branches for k6"
        type: string
        default: "test,main"
      run_deploy:
        description: "Enable deployment"
        type: boolean
        default: true
      run_promotion:
        description: "Enable PR promotions"
        type: boolean
        default: true
      dry_run:
        description: "Preview, skip deploy/tag push"
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  packages: write
  security-events: write

jobs:
  call-backend-orchestrator:
    # Track main so caller repos receive central workflow fixes immediately.
    uses: cicd-external-project/central-workflow/.github/workflows/master-pipeline-be.yml@main
    with:
      pipeline_mode: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.pipeline_mode || 'auto' }}
      enable_security_scan: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.enable_security_scan || 'true') || github.event_name != 'workflow_dispatch' }}
      enable_sonar: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.enable_sonar || 'true') || github.event_name != 'workflow_dispatch' }}
      enable_k6: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.enable_k6 || 'false') || false }}
      k6_script_path: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.k6_script_path || 'tests/performance' }}
      k6_base_url: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.k6_base_url || '' }}
      k6_run_only_on_branch: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.k6_run_only_on_branch || 'test,main' }}
      run_deploy: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.run_deploy || 'true') || github.event_name == 'push' }}
      run_promotion: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.run_promotion || 'true') || github.event_name == 'push' }}
      dry_run: ${{ github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.dry_run || 'false') || false }}
    secrets: inherit
